services:
  nginx:
    image: nginx:alpine
    container_name: uastudio-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - /home/n3/Proj/uastudio.site/dist/base:/usr/share/nginx/html:ro
